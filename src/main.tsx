import React from 'react';
import ReactDOM from 'react-dom/client';
import RogueWheel from './RogueWheel';

ReactDOM.createRoot(document.getElementById('root') as HTMLElement).render(
  <React.StrictMode>
    <RogueWheel />
  </React.StrictMode>
);
